# spring-boot-movies-tracker-app
Movies tracker application with spring boot :p</br>
To check the tables creation go to http://localhost:8080/h2-console and JDBC URL will be jdbc:h2:mem:testdb (the same one as the one configured in application.properties).
